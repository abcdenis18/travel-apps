import 'package:flutter/material.dart';

class ExploresView extends StatelessWidget {
  const ExploresView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Explore View'),
      ),
      body: Container(),
    );
  }
}
